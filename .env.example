# YiPet Environment Variables
# Copy this file to .env and fill in your actual values.
# .env is gitignored — never commit real tokens.

# API 认证 Token — 必填
# 在扩展控制面板中也可以设置，环境变量是最高优先级
API_X_TOKEN=your-api-token-here
